# Пример команды msg.
# Эта команда показывает обычное окно сообщения Windows.
# Полезно, если нужно быстро показать текст, значение переменной
# или результат вычисления без вывода в консоль.
# При запуске этого файла появятся два окна подряд.

msg "hello from msg"
msg "random: " + random(100)
